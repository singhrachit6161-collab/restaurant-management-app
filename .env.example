DATABASE_URL="file:./dev.db"
AUTH_SECRET="change-me"
NEXTAUTH_URL="http://localhost:3000"
AUTH_TRUST_HOST=true

# Optional: enables the "AI Insights" card on /dashboard (Claude API).
# Leave unset to have that feature show a graceful "unavailable" message instead.
ANTHROPIC_API_KEY=""
